# List

This is a generic list component. It can be used to avoid re-implementing list style overrides whenever a list is required. It also helps avoid having components that make opinions about their parent (components that use the li tag as root).