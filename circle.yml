general:
  artifacts:
    - "./lib"

machine:
  node:
    version: 6.9.1
  post:
    - npm install -g npm@3.7.3

dependencies:
  override:
    - npm install

test:
  override:
    - npm run lint
    - npm run typecheck
    - npm run test -- --coverage
