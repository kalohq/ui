general:
  artifacts:
    - "./lib"

machine:
  node:
    version: 8.9.3

dependencies:
  override:
    - npm install

test:
  override:
    - npm run lint
    - npm run typecheck
    - npm run test -- --coverage
    - bash <(curl -s https://codecov.io/bash)
